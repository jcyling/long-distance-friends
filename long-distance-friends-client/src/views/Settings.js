import React from "react";

const Settings = ({ user }) => {
  return (
    <div>
      {/* Insert account settings edit form */}
      <h1>Looks like something went wrong.</h1>
    </div>
  );
};

export default Settings;